services:
  web-app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./djangoProject:/djangoProject

    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"